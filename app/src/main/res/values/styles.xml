<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="Theme.RetrofitDemo" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryDark">@color/purple_700</item> <!-- Replaces colorPrimaryVariant -->
        <item name="android:textColorPrimary">@color/white</item> <!-- Replaces colorOnPrimary -->
        <!-- Other theme attributes -->
    </style>
</resources>